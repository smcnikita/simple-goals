import type { Theme } from '@/types/theme';

export const DEFAULT_APP_THEME: Theme = 'system';
export const SUPPORTED_THEMES: Theme[] = ['light', 'system', 'dark'];
export const THEME_PREFIX = '';
export const THEME_STORAGE_KEY = 'theme';
