import type { Theme } from '@/types/theme';

export const DEFAULT_APP_THEME: Theme = 'dark';
export const SUPPORTED_THEMES: Theme[] = ['light', 'dark'];
export const THEME_PREFIX = 'theme_';
export const THEME_STORAGE_KEY = 'theme';
